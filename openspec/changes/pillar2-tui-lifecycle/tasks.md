# Tasks: Pillar 2 — Interactive TUI & Lifecycle Operations

Ordering rule (openspec `strict_tdd`): every phase starts by writing that phase's failing offline tests, then implements until they pass.

## Phase 1: Lifecycle Foundations (Upgrader & Uninstaller)

- [x] 1.1 Write failing tests in `test/updater.test.ts`: install-method detection from `process.execPath` (standalone iff the real path is under `~/.prhero/bin/`; source checkout iff `resolveVersion()` reports `dev`; npm/bun otherwise) and the shadow-install warning (other installs detected, warned about, never touched); Phase A plan (download `pr-hero-<target>` to a sibling temp file of the target → SHA256 verification against `SHA256SUMS` → `chmod +x` → preserve `pr-hero.bak` → atomic `renameSync`); source-checkout informative no-op; `--yes` / `--dry-run`
- [x] 1.2 Write failing tests for the `.bak` lifecycle: `.bak` kept until reconciliation succeeds, then removed; automatic `.bak` restore when the re-exec of the new binary fails to start; a later `upgrade` run detecting leftover `.bak`/incomplete state and resuming or cleaning idempotently
- [x] 1.3 Write failing tests for `pr-hero upgrade --check` and the cache: `--check` always performs a fresh query and rewrites `~/.prhero/upgrade-check.json`; the 24h TTL governs only passive refresh on plain `upgrade` runs; the menu path never queries the network (cache read only); `upgradeCheckPath` added to `PrheroLayout`; the reconciled version recorded in the cache and the doctor "reconciliation pending — run pr-hero upgrade --reconcile" flag on a version/reconciled mismatch
- [x] 1.4 Write a failing test pinning the canonical repo constant to `Gentleman-Programming/pr-hero` (matching `README.md` and `install.sh`)
- [x] 1.5 Write failing tests for the re-exec reconcile step: `upgrade --reconcile` runs `syncSkills({ force: true })`, verifies MCP registrations, opens the store once (migrations auto-run via `PRAGMA user_version`), reloads launchd daemons only on macOS with plists installed, runs a quick doctor pass; reconciliation is invoked via re-exec of the new binary on both the standalone and npm/bun paths; reconcile-step failures do not roll back the binary (failing step printed, re-run instructed)
- [x] 1.6 Write failing tests for the agent-env inverse functions: MCP unregistration and skills removal driven by the skills digest (no hardcoded skill names), digest-verified per file — only files whose content hash matches the recorded digest are removed; a modified file is left in place with a notice
- [x] 1.7 Write failing tests in `test/uninstaller.test.ts` for the pure plan generator, program/data split: program side — both launchd labels (`io.prhero.watch`, `io.prhero.gc`) scoped to macOS with skip notices elsewhere; digest-verified MCP/skills inverse steps; rc-file PATH-line cleanup (`.prhero/bin` exact-line matching in `~/.zshrc`, `~/.bashrc`, `~/.bash_profile`, and the fish config) with the manual-instruction fallback; npm/bun global-removal offer; standalone binary + `~/.prhero/bin/` removal only when the running install is standalone. Data side (`--purge`) — coverage (`prhero.db`, metrics db, store socket, `watch.json`, `config.json`, `setup.json`, `upgrade-check.json`, `active_runs/`, logs, repos dir — the remainder of `~/.prhero/`); dual pre-purge gate (`active_runs` live entries after stale pruning + `lockHolder`); in-repo `.prhero/` prompt (team-property copy, default No, never removed with `--yes`); plan shown as the confirmation screen; `--dry-run`
- [x] 1.8 Implement `src/updater.ts` (execPath detection, Phase A with sibling temp + `.bak`, `--check`, `--reconcile`, resume/clean) and add `upgradeCheckPath` to `PrheroLayout` in `src/home-preflight.ts`
- [x] 1.9 Implement the inverse functions in `src/agent-env.ts` (digest-verified per file)
- [x] 1.10 Implement `src/uninstaller.ts` (pure plan generator + side-effect runner, program/data split)
- [x] 1.11 Wire CLI subcommands `pr-hero upgrade` (including `--check`) and `pr-hero uninstall` with `--yes`, `--dry-run`, `--purge`; make all Phase 1 tests pass

## Phase 2: Active Runs Tracker & Store Activity Monitor

- [x] 2.1 Write failing tests in `test/activity.test.ts`: registry lifecycle (file created at review launch, removed on exit; stale dead-PID entries pruned on read using the `lockHolder` liveness semantics — `kill(pid, 0)`, `EPERM` counts as alive); the driver SIGTERM handler (tears down its live agent subprocesses via their `StepHandle.kill()` handles, removes its registry entry, exits non-zero); the kill path with a fake signal sender — identity check via `ps -o command= -p <pid>` (mismatch prunes the stale entry and refuses without signalling), SIGTERM to the PID only on match, the 10s bounded escalation to SIGKILL with the honest survivor report, and the confirmation gate (`--yes` skips only the confirm — never the identity check); recent-history query from the `runs` table (`run_status`, `blocking`/`advisory`, `wall_ms`, `cost_usd_est`, `generated_at`) including the empty state (absent db, zero rows) and the 10-row cap; the watcher spend read (`countLaunchedToday` + `dailyCap`)
- [x] 2.2 Write failing renderer tests in `test/ui-activity.test.ts`: active runs list with the select+Enter kill affordance, spend line (launches-used/daily-cap), history table capped at 10, empty state — exact lines with styles on and off, including a test asserting zero `\x1b` bytes with styles off; the sub-surface key contract (`r` re-gathers and re-renders, `q`/`Esc` return to the root menu, `Ctrl-C` exits 130)
- [x] 2.3 Implement `src/activity.ts` (registration, liveness probing, identity check, signalling with escalation, store query, spend read)
- [x] 2.4 Wire review start/finish hooks and the driver SIGTERM handler in `src/cli.ts` and `src/watch.ts` to register active runs and tear down cleanly
- [x] 2.5 Implement `src/ui-activity.ts` (render active runs list, kill confirmation, spend line, and recent store history table)
- [x] 2.6 Wire `pr-hero activity` and `pr-hero activity --kill <pid>` (with `--yes`); make all Phase 2 tests pass

## Phase 3: Menu Context & Pure UI Renderers

- [x] 3.1 Write failing tests in `test/menu-context.test.ts`: `resolveMenuContext` classification; `menuOptions` per context (Review/Ledger omitted outside a configured repo; Init first in an unconfigured repo; Activity present in every context; the Lifecycle group present in every context with `Upgrade & sync`, `Sync skills & MCP` mapped to the existing `setup` action, and `Managed uninstall`; no Reset/Vacuum item)
- [x] 3.2 Write failing renderer tests in `test/ui-menu.test.ts`: exact lines for `renderSolidHeader`, `renderContextBox`, `renderMenuCard`, and the persistent footer (key hints + the selected item's CLI equivalent; submenu-openers show the submenu hint) with styles on and off, including a test asserting zero `\x1b` bytes with styles off; the width tiers (full layout at >= 60, plain one-line title for 24-59) with no horizontal overflow at width >= 24; status badges rendered from `MenuStatusInfo` (Upgrade state on the Lifecycle group label)
- [x] 3.3 Write failing tests for the `box()` border-style option in `src/ui.ts`: default `"round"` leaves existing callers' output unchanged; `"double"` draws `╔ ╗ ╚ ╝ ═ ║`
- [x] 3.4 Write failing tests for the control-byte sanitizer in `src/ui.ts`: strips `0x00-0x1F`, `0x7F`, and ESC sequences from externally-sourced strings; pure, with its own test file coverage
- [x] 3.5 Implement `src/menu-context.ts` (`resolveMenuContext` and dynamic `menuOptions` with live status badges and the Lifecycle group)
- [x] 3.6 Extend `src/ui.ts` with the border-style option and the sanitizer
- [x] 3.7 Implement `src/ui-menu.ts` (`renderSolidHeader` with tiers, `renderContextBox`, `renderMenuCard`, footer); make all Phase 3 tests pass

## Phase 4: Keyboard Foundations & Repo-Optional Commands

- [x] 4.1 Write failing tests for the `parseKey` extension in `src/ui-select.ts`: `Esc` and `Ctrl-C` become distinguishable key types; `splitKeys` behavior unchanged; `runConfirm` keeps treating both as cancel (compatible extension; existing tests updated)
- [x] 4.2 Write failing tests for repo-optional `pr-hero doctor`: outside a git repository it runs system checks only and exits successfully (today it throws via `resolveRepoRoot`)
- [x] 4.3 Write failing tests for repo-optional `pr-hero config`: outside a git repository it reads/writes the global layers only (Person, Watch) and stays pipeable
- [x] 4.4 Write failing tests for repo-optional `pr-hero setup`: outside a git repository it runs the machine-level steps only (tools probe, skills sync, MCP registration, `setup.json`) and skips repo steps with a notice
- [x] 4.5 Extend `parseKey`; make `doctor`, `config`, and `setup` repo-optional in `src/cli.ts`; make all Phase 4 tests pass

## Phase 5: Root Dispatch, Submenus & Review Submenu

- [x] 5.1 Write failing input-loop tests (injected `KeyReader`/io per the `src/ui-select.ts` pattern): `j`/`k` and `↑`/`↓` move with wrapping; digits `1-9` move the selection and do not execute; `Enter` executes; `q`/`Esc` exit 0 at the root and mean "back" inside sub-surfaces; `Ctrl-C` exits 130 everywhere; raw mode restored (reader closed in `finally`)
- [x] 5.2 Write failing tests for the single keyboard owner: the root closes its reader before dispatching any action or interactive sub-surface, the sub-surface opens and closes its own, the root re-creates its reader and re-renders on return; the `SIGWINCH` listener is registered at loop start, triggers a redraw with a fresh `terminalWidth()`, and is removed in the same `finally` that closes the reader
- [x] 5.3 Write failing dispatch tests in `test/cli.test.ts`: zero args + TTY pair + onboarded opens the menu and starts no review implicitly (the implicit `review` default is removed); zero args + non-TTY stdin prints help and exits 2; stdin TTY + redirected stderr prints help and exits 2 (zero-arg and `pr-hero menu`); TTY + not onboarded runs the wizard; `PRHERO_NO_TUI` set (any value, including empty) prints help and exits 0; width below 24 columns refuses with "terminal too narrow", help, exit 2
- [x] 5.4 Write failing dispatch-matrix tests: every menu item dispatches its matrix command in its matrix contexts; returning rows pause ("press any key") and re-render the root menu; terminal rows (review after the spend confirm, upgrade, uninstall, quit) do not return; the watcher submenu offers status/install/uninstall/remove everywhere and add + on-push in-repo only; bare `pr-hero watch` is never dispatched; the Activity item opens its interactive sub-surface (the matrix command is the headless twin/footer hint, not a literal dispatch); the Lifecycle submenu dispatches `upgrade`, `setup`, and `uninstall`
- [x] 5.5 Write failing review-submenu tests: collects target (branch vs PR) and toggles (`post` only when the target is a PR, `scout`, `force`, summary on/off, model presets or free text); hands the resulting options to the existing review flow (size gate + cost-band confirm); renders no cost UI itself; renderer tests include one asserting zero `\x1b` bytes with styles off
- [x] 5.6 Wire zero-arg dispatch and `pr-hero menu` in `src/cli.ts` (TTY pair gate, width floor, stderr rendering via `log()`, `styleEnabled(process.stderr)`, `terminalWidth()`); remove the implicit zero-arg `review` mapping
- [x] 5.7 Implement the keyboard loop, ownership handoff, and `SIGWINCH` redraw + cleanup in `src/ui-menu.ts`; implement `gatherMenuStatus` (impure, in the shell; reuses doctor evaluators; the Upgrade chip reads only the cache)
- [x] 5.8 Implement `src/ui-review-menu.ts`, the watcher submenu, and the Lifecycle submenu; connect all menu actions per the dispatch matrix; make all Phase 5 tests pass

## Phase 6: Interactive Config Editor, `config set` & `config unset`

- [ ] 6.1 Write failing tests in `test/ui-config-edit.test.ts`: exact key sets per layer (Person: `agents_dir`, `summary.model`, `summary.enabled`, `max_verification_steps`; Team: `default_base`, `parity_trigger_paths`, `suspicion_priors`, `summary.enabled`, `max_verification_steps`; Watch: `daily_cap`, `window`, per-repo `post`, `on_push`, `max_changed_lines`, `max_changed_files`); accept-and-annotate for capped Team keys — an over-ceiling type-valid value is written and the effective-value annotation from `mergeConfig` is displayed ("written: 5 — your effective value remains 3, capped by your Person layer"); write-time validation is types/shape only through the existing parsers; validation flowing through `CONFIG_DIRECTION`/`SUMMARY_DIRECTION` + `mergeConfig` (no reimplemented direction logic); Team layer disabled outside a repo; renderer tests include one asserting zero `\x1b` bytes with styles off
- [ ] 6.2 Write failing tests for `pr-hero config set <key> <value> [--person|--team|--watch]`: the v1 scalar grammar exactly (`summary.enabled` true/false, `summary.model` string, `max_verification_steps` non-negative integer, `agents_dir` path string, `default_base` string, `--watch daily_cap <n>` positive integer, `--watch window <HH:MM-HH:MM>` parsed to `{start, end}`); defaults to `--person`; `--team` outside a repository fails with a descriptive error; an over-ceiling capped Team value is accepted and annotated; a valid set writes 2-space-indented JSON; bare `pr-hero config` remains read-only and pipeable
- [ ] 6.3 Write failing tests for `pr-hero config unset <key> [--person|--team|--watch]`: removes the key from the layer with 2-space JSON write-back; unsetting `window` means "always"
- [ ] 6.4 Implement `src/ui-config-edit.ts` for Person, Team, and Watcher layers (accept-and-annotate)
- [ ] 6.5 Implement `pr-hero config set` and `pr-hero config unset` over the same validation machinery and wire `pr-hero config --edit` plus the menu option; make all Phase 6 tests pass

## Phase 7: Verification & Documentation

- [ ] 7.1 Run full offline test suite (`bun test`)
- [ ] 7.2 Run typecheck (`bun run typecheck`) and Biome linter (`bun run check`)
- [ ] 7.3 Update `README.md` and the CLI help: document the removal of the implicit zero-argument `review` default as a breaking change, the new verbs (`menu`, `upgrade` including `--check`, `uninstall`, `activity`, `config set`/`config unset`), the repo-optional behavior of `doctor`/`config`/`setup`, and the `PRHERO_NO_TUI` escape hatch
